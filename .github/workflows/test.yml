name: test

on:
  push:
    branches:
    - master
    - tidy
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build wheels on ${{ matrix.os }} ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    environment: TEST_PYPI_API_TOKEN
    strategy:
      matrix:
        python-version: ['3.7']
        os: [ubuntu-20.04]
    steps:
    - uses: actions/checkout@v2

    - uses: actions/setup-python@v2
      with:
          python-version: ${{ matrix.python-version }}

    - name: Install snipar
      run: python setup.py install

    - name: run tests
      run: python -m unittest snipar.tests